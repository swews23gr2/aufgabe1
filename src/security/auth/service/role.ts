/**
 * Das Modul besteht aus den Rollen {@linkcode Role} für die
 * Autorisierung (RBAC = role based access control).
 * @packageDocumentation
 */

export type Role = 'admin' | 'student' | 'fachabteilung';
